export namespace CoreActions {
  export class ShowSpinner {
    public static readonly type = '[Core] Show spinner';
  }

  export class HideSpinner {
    public static readonly type = '[Core] Hide spinner';
  }
}
