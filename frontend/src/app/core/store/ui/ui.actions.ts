export namespace UIActions {
  export class ShowSpinner {
    public static readonly type = '[UI] Show spinner';
  }

  export class HideSpinner {
    public static readonly type = '[UI] Hide spinner';
  }
}
